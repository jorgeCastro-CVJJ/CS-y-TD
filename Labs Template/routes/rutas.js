const express = require('express');
const router = express.Router();


const controlador = require('path del controlador')



router.get('/ruta', controlador.metodo)
router.post('/ruta', controlador.metodo)